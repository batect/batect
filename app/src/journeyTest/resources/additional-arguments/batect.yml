project_name: additional-arguments-test

containers:
  build-env:
    image: alpine:3.16.0

tasks:
  the-task:
    run:
      container: build-env
      command: echo "This is the output from the config file."
